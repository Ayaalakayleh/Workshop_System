@using Workshop.Resources
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<Common> Common


<div class="wl-side">
    <div class="card">
        <div class="card-header d-flex justify-content-between">
            <h5 class="mb-0">@Common["Label_UnscheduledLabour"] </h5>
            <!-- NEW: Groups button -->
            <button id="btnOpenGroups" class="btn btn-sm btn-outline-info ms-2">
                <i class="fas fa-layer-group"></i> @Common["Label_Groups"]
            </button>
        </div>
        <div class="card-body">
            <div id="unscheduledList" class="d-flex flex-column gap-2"></div>
        </div>

        <div class="hiddenIds">
        </div>
    </div>
</div>